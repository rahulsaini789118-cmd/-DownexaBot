version: "3.8"

services:
  mediafetch-bot:
    build: .
    container_name: mediafetch_bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./downloads:/app/downloads
      - ./logs:/app/logs
